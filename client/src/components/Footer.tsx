export function Footer() {
  return (
    <footer className="border-t border-ink/15 bg-muted">
      <div className="mx-auto flex max-w-7xl flex-col gap-3 px-5 py-8 text-sm text-ink/70 md:flex-row md:items-center md:justify-between md:px-8">
        <p className="font-semibold text-ink">FlexiBuy assignment demo</p>
        <p>No real purchases or financial applications are processed.</p>
      </div>
    </footer>
  );
}
